TODO for this branch only.

Done:  

marked %p/Library/Python as a legal directory in the validator.

Validator check for objectionable items in RuntimeVars.  In this case, that'd be 
PYTHONPATH, but there are probably other things we don't want to set globally,
e.g. DYLD_LIBRARY_PATH.